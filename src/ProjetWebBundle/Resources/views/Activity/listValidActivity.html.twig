{% extends "::layout/layout.html.twig" %}

{% block body %}
    <section id="sec">
        <p>-</p>
        <div class="row">
            <div class="col-lg-offset-2 col-lg-8 fond">

                <h1 style="text-align: center">Liste des activitées validées</h1>

                <div class="col-lg-12">

                    {% for activity in listValidActivity %}
                        <h2><strong> {{ activity.activityName }}
                                : {{ activity.date|date("m/d/Y")|date('Y-m-d') }}</strong></h2>
                        <h4><br></h4>
                        <h4>{{ activity.description }}</h4>
                        {% if activity.price == 0 %}
                            <h4><p>
                                    L'activité est <strong>gratuite</strong></p></h4>
                        {% else %}
                            <h4><p> Le prix est de :
                                    {{ activity.price }} €</p></h4>
                        {% endif %}




                        <h4><a class="btn btn-danger" href="{{ path('add_photo', {'activityId' : activity.id}) }}">Détails
                                de l'activité</a> <a class="btn btn-danger" href="">Participer à l'activité</a></h4>

                        <br>

                    {% endfor %}

                    <h4><a class="btn btn-danger" href="{{ path('create_activity') }}">Soumettre une activité</a></h4>

                </div>

            </div>
        </div>
    </section>

{% endblock %}