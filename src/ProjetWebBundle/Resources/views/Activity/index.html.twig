{% extends "::layout/layout.html.twig" %}

{% block body %}
    <section id="sec">
        <p>-</p>
        <div class="row">
            <div class="col-lg-offset-2 col-lg-8 fond">
                <br>
                <h1>Bienvenue sur le site OFFICIEL du BDE de l'Exia Cesi Strasbourg</h1>
                <br>
                <h3>Le BDE est le bureau des élèves, il s'occupe de représenter officiellement les élèves et les associations.
                Il aide également les élèves à volariser leurs projet et supervise les manifestations externes et internes.</h3>
                <br>
                <h2><u>Derniers évenements :</u></h2>
                {% for activity in listValidActivity %}
                    <h3><strong> {{ activity.activityName }} : {{ activity.date|date("m/d/Y")|date('Y-m-d') }}</strong>
                        <a
                                class="btn btn-danger"
                                href="{{ path('add_photo', {'activityId' : activity.getId()}) }}">Détails de
                            l'activité</a></h3>

                    <h4>{{ activity.description }}</h4>

                    <br>


                {% endfor %}

                <h4><a class="btn btn-danger" href="{{ path('list_Valid_activity') }}">Liste des activitées</a></h4>
            </div>
        </div>

    </section>
{% endblock %}